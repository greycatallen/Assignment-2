# Results

Copy the evidence you want to publish from `pacman_runs/<run>/` into this folder:

- `comparison.json` — the five before/after evaluation scores
- `config.json` — settings, hardware, and package versions
- the training dashboard plot
- the untrained and final gameplay GIFs

Large checkpoints (`*.pt`) stay out of git; keep them locally or attach them to a release.
